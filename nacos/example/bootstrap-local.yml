application:
  name: "hello"
  env: "local"
nacos:
  server: # 可以添加多个server
    - { ipAddr: "localhost", port: 8848 }
  client: # 参考ClientConfig的配置，这里只列出必须的配置
    username: "nacos"
    password: "nacos"
    namespaceId: "" # public可以填空字符串
